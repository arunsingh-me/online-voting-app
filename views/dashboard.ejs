<h1 class="mt-4">Dashboard</h1>
<p class="lead mb-3">Welcome <%= user.name %></p>
<h4 id="email">Your email: <%=user.email%></h4>
<a href="/users/logout" class="btn btn-secondary">Logout</a>
<a href="/vote.html" class="btn btn-primary">Proceed to Vote</a>
<script>
    const email = document.getElementById('email').textContent;
    // const count = JSON.parse(localStorage.getItem('key')).count || 0;
    // const user = {
    //     email: email,
    //     count: count
    // };
    // console.log(user);
    const count = localStorage.getItem(email) ?? 0;
    localStorage.setItem(email, count);
    sessionStorage.setItem('key', email);

    console.log(email);
    console.log(count);
</script>
